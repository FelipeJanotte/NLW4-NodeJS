
<style>
    .container{
        width: 800px;
        justify-content: center;
        align-items: center;
        align-content: center;
        display: flex;
        flex-direction: column;
    }

    .level{
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 350px;
    }

    .answers{
        width: 350px;
        display: flex;
        justify-content: space-between;
    }

    .value{
        padding: 10px;
        background-color: #8257e6;
        color: #fff;
        width: 15px;
        text-align: center;
        text-decoration: none;
        transition: .35s;
    }
    .first-value{
        border-radius: 6px 0 0 6px;
    }
    .last-value{
        border-radius: 0 6px 6px 0;
    }
    .value:hover{
        background-color: #543699;
    }

</style>

<div class="container">
    <label>Olá <strong>{{name}}</strong>! Tudo bem?</label>

    <h3>{{title}}</h3>
    <br />
    <strong>{{description}}</strong>

    <div class="level">
        <span>Pouco provável</span>
        <span>Muito provável</span>
    </div>

    {{!-- https://localhost:3333/answers/${nota}?u={id_usuario} --}}
    <div class="answers">
        <a class="value first-value" href="{{link}}/1?u={{user_id}}">1</a>
        <a class="value" href="{{link}}/2?u={{user_id}}">2</a>
        <a class="value" href="{{link}}/3?u={{user_id}}">3</a>
        <a class="value" href="{{link}}/4?u={{user_id}}">4</a>
        <a class="value" href="{{link}}/5?u={{user_id}}">5</a>
        <a class="value" href="{{link}}/6?u={{user_id}}">6</a>
        <a class="value" href="{{link}}/7?u={{user_id}}">7</a>
        <a class="value" href="{{link}}/8?u={{user_id}}">8</a>
        <a class="value" href="{{link}}/9?u={{user_id}}">9</a>
        <a class="value last-value" href="{{link}}/10?u={{user_id}}">10</a>
    </div>

    <br/>
    <br/>

    <strong>Sua opnião é muito importante para nós!</strong>
    <h3>Equipe | <strong>NLW</strong></h3>
</div>